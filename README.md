Sources for the plugin `CoronaProvider.native.popup.safariView`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "CoronaProvider.native.popup.safariView" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
